services:
  - type: web
    name: eboa-website
    runtime: node
    plan: free
    buildCommand: npm install && npm run db:init && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: DATABASE_URL
        sync: false
